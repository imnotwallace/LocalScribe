<ui:FluentWindow x:Class="LocalScribe.App.SessionDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:vm="clr-namespace:LocalScribe.App.ViewModels"
        Title="Session details" Height="640" Width="460" MinHeight="400" MinWidth="380"
        Icon="pack://application:,,,/Assets/LocalScribe.ico"
        WindowBackdropType="Mica" ExtendsContentIntoTitleBar="True">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </Window.Resources>
    <ui:FluentWindow.InputBindings>
        <!-- Stage 5.4 5.1: keyboard save. Title/Description below bind PropertyChanged, so the
             working copy is already current when this fires; the two count TextBoxes keep
             LostFocus (mid-typing int parse) and are force-committed by the close guard. -->
        <KeyBinding Modifiers="Control" Key="S" Command="{Binding SaveCommand}" />
    </ui:FluentWindow.InputBindings>
    <DockPanel TextElement.Foreground="{DynamicResource TextFillColorPrimaryBrush}">
        <ui:TitleBar DockPanel.Dock="Top" Title="Session details"
                     ShowMinimize="True" ShowMaximize="True" ShowClose="True">
            <ui:TitleBar.Icon>
                <ui:ImageIcon Source="pack://application:,,,/Assets/LocalScribe.ico" />
            </ui:TitleBar.Icon>
        </ui:TitleBar>
        <ScrollViewer x:Name="DetailPane" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="16">
                <!-- Lifted verbatim (minus the drawer's close button) from the interim drawer in
                     Pages/SessionsPage.xaml (design 3.3) - this window IS the detail view, so there
                     is no card-visibility trigger and no close affordance beyond the title bar. -->
                <DockPanel>
                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="Unsaved changes" Style="{StaticResource MutedText}"
                                   VerticalAlignment="Center" Margin="0,0,8,0"
                                   Visibility="{Binding IsDirty, Converter={StaticResource BoolToVis}}" />
                        <ui:Button Content="Discard" Appearance="Secondary"
                                   Command="{Binding DiscardCommand}" Margin="0,0,8,0" />
                        <ui:Button Content="Save" Appearance="Primary"
                                   Command="{Binding SaveCommand}" />
                    </StackPanel>
                    <TextBlock Text="Details" FontWeight="SemiBold" FontSize="16" VerticalAlignment="Center" />
                </DockPanel>
                <TextBlock Text="{Binding LockHint}" Style="{StaticResource MutedText}" Margin="0,4,0,0" />

                <StackPanel IsEnabled="{Binding IsEditable}">
                    <!-- Session -->
                    <ui:Card Style="{StaticResource SectionCard}" Margin="0,8,0,12">
                        <StackPanel>
                            <TextBlock Text="Session" FontWeight="SemiBold" Margin="0,0,0,8" />
                            <TextBlock Text="Title" Margin="0,0,0,4" />
                            <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" />
                            <TextBlock Text="Description" Margin="0,12,0,4" />
                            <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                                     AcceptsReturn="True" MinLines="3" TextWrapping="Wrap" />
                        </StackPanel>
                    </ui:Card>

                    <!-- Classification -->
                    <ui:Card Style="{StaticResource SectionCard}">
                        <StackPanel>
                            <TextBlock Text="Classification" FontWeight="SemiBold" Margin="0,0,0,8" />
                            <TextBlock Text="Call type" Margin="0,0,0,4" />
                            <ComboBox ItemsSource="{x:Static vm:MetadataEditorViewModel.MediumOptions}"
                                      DisplayMemberPath="Display" SelectedValuePath="Value"
                                      SelectedValue="{Binding SelectedMedium}" />
                            <DockPanel Margin="0,12,0,4">
                                <CheckBox DockPanel.Dock="Right" Content="Show archived"
                                          IsChecked="{Binding ShowArchivedMatters}" />
                                <TextBlock Text="Matters" />
                            </DockPanel>
                            <ItemsControl ItemsSource="{Binding MatterOptions}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <CheckBox Content="{Binding Display}" IsChecked="{Binding IsSelected, Mode=OneWay}"
                                                  Command="{Binding DataContext.ToggleMatterCommand,
                                                            RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                  CommandParameter="{Binding}" />
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </ui:Card>

                    <!-- Participants: two-column Local/Remote speaker manager (Stage 5.2 Task 6).
                         Roster-add per side is Task 7 - "Add from roster" below stays flat for now. -->
                    <ui:Card Style="{StaticResource SectionCard}">
                        <StackPanel>
                            <TextBlock Text="Participants" FontWeight="SemiBold" Margin="0,0,0,8" />
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" Margin="0,0,6,0">
                                    <TextBlock Text="Local" FontWeight="SemiBold" Margin="0,0,0,4" />
                                    <ItemsControl ItemsSource="{Binding LocalParticipants}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <DockPanel Margin="0,2,0,2">
                                                    <ui:Button DockPanel.Dock="Right" Content="Remove" Appearance="Secondary"
                                                               Command="{Binding DataContext.RemoveParticipantCommand,
                                                                         RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                               CommandParameter="{Binding}" />
                                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                        <TextBlock Text="{Binding Name, Mode=OneWay}" />
                                                        <TextBlock Text=" (you)" Margin="4,0,0,0" Style="{StaticResource MutedText}"
                                                                   Visibility="{Binding IsSelf, Converter={StaticResource BoolToVis}}" />
                                                    </StackPanel>
                                                </DockPanel>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                    <DockPanel Margin="0,6,0,0">
                                        <ui:Button DockPanel.Dock="Right" Content="Add" Appearance="Primary"
                                                   Command="{Binding AddLocalNameCommand}" Margin="6,0,0,0" />
                                        <ui:TextBox PlaceholderText="Add local speaker"
                                                    Text="{Binding NewLocalName, UpdateSourceTrigger=PropertyChanged}" />
                                    </DockPanel>
                                    <!-- Task 7: per-side roster add (shared SelectedRosterPick, Local side) -->
                                    <DockPanel Margin="0,6,0,0">
                                        <ui:Button DockPanel.Dock="Right" Content="Add from roster" Appearance="Secondary"
                                                   Command="{Binding AddLocalFromRosterCommand}" Margin="6,0,0,0" />
                                        <ComboBox ItemsSource="{Binding RosterPicks}" DisplayMemberPath="Display"
                                                  SelectedItem="{Binding SelectedRosterPick, Mode=TwoWay}" />
                                    </DockPanel>
                                </StackPanel>
                                <StackPanel Grid.Column="1" Margin="6,0,0,0">
                                    <TextBlock Text="Remote" FontWeight="SemiBold" Margin="0,0,0,4" />
                                    <ItemsControl ItemsSource="{Binding RemoteParticipants}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <DockPanel Margin="0,2,0,2">
                                                    <ui:Button DockPanel.Dock="Right" Content="Remove" Appearance="Secondary"
                                                               Command="{Binding DataContext.RemoveParticipantCommand,
                                                                         RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                               CommandParameter="{Binding}" />
                                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                        <TextBlock Text="{Binding Name, Mode=OneWay}" />
                                                        <TextBlock Text=" (you)" Margin="4,0,0,0" Style="{StaticResource MutedText}"
                                                                   Visibility="{Binding IsSelf, Converter={StaticResource BoolToVis}}" />
                                                    </StackPanel>
                                                </DockPanel>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                    <DockPanel Margin="0,6,0,0">
                                        <ui:Button DockPanel.Dock="Right" Content="Add" Appearance="Primary"
                                                   Command="{Binding AddRemoteNameCommand}" Margin="6,0,0,0" />
                                        <ui:TextBox PlaceholderText="Add remote speaker"
                                                    Text="{Binding NewRemoteName, UpdateSourceTrigger=PropertyChanged}" />
                                    </DockPanel>
                                    <!-- Task 7: per-side roster add (shared SelectedRosterPick, Remote side) -->
                                    <DockPanel Margin="0,6,0,0">
                                        <ui:Button DockPanel.Dock="Right" Content="Add from roster" Appearance="Secondary"
                                                   Command="{Binding AddRemoteFromRosterCommand}" Margin="6,0,0,0" />
                                        <ComboBox ItemsSource="{Binding RosterPicks}" DisplayMemberPath="Display"
                                                  SelectedItem="{Binding SelectedRosterPick, Mode=TwoWay}" />
                                    </DockPanel>
                                </StackPanel>
                            </Grid>
                            <!-- Task 7 fixed the "everything is remote" bug: roster-add now stamps the
                                 column's side (the two per-side ComboBox+button pairs above), replacing
                                 the old flat Remote-only "Add from roster". Both bind the single shared
                                 SelectedRosterPick - pick once, then click the target column's Add. -->
                        </StackPanel>
                    </ui:Card>

                    <!-- Speakers -->
                    <ui:Card Style="{StaticResource SectionCard}">
                        <StackPanel>
                            <TextBlock Text="Speakers" FontWeight="SemiBold" Margin="0,0,0,8" />
                            <!-- Stage 5.3 Task 7: Split speakers relocated here from the Sessions-list
                                 context menu (retired). Disabled for a pending/in-progress row via the
                                 VM's CanDiarise gate (G7) - not just a no-op click. -->
                            <ui:Button Content="Split speakers..." Command="{Binding DiariseCommand}"
                                       HorizontalAlignment="Right" Margin="0,0,0,8" />
                            <!-- Task 7: counts DERIVE from the Local/Remote lists by default. Unchecking
                                 is the system-mix override (declared speaker count != named participants);
                                 the count boxes become editable only then (IsEnabled=CountsAreManual, the
                                 VM's computed inverse - the app has no inverse-bool converter). -->
                            <CheckBox Content="Speaker counts follow these lists"
                                      IsChecked="{Binding CountsFollowLists}" Margin="0,0,0,8" />
                            <UniformGrid Columns="2">
                                <StackPanel Margin="0,0,6,0">
                                    <TextBlock Text="Local speakers" Margin="0,0,0,4" />
                                    <TextBox Text="{Binding LocalCount, UpdateSourceTrigger=LostFocus}"
                                             IsEnabled="{Binding CountsAreManual}" />
                                </StackPanel>
                                <StackPanel Margin="6,0,0,0">
                                    <TextBlock Text="Remote speakers" Margin="0,0,0,4" />
                                    <TextBox Text="{Binding RemoteCount, UpdateSourceTrigger=LostFocus}"
                                             IsEnabled="{Binding CountsAreManual}" />
                                </StackPanel>
                            </UniformGrid>
                            <CheckBox Content="Archived (hidden from the default list)"
                                      IsChecked="{Binding Archived}" Margin="0,12,0,0" />
                        </StackPanel>
                    </ui:Card>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </DockPanel>
</ui:FluentWindow>
