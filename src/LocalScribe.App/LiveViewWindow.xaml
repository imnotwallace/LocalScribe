<ui:FluentWindow x:Class="LocalScribe.App.LiveViewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="LocalScribe - Record console" Height="520" Width="640" MinHeight="300" MinWidth="420"
        WindowBackdropType="None" ExtendsContentIntoTitleBar="True">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </Window.Resources>
    <Window.InputBindings>
        <!-- In-app mirror of Teams' mute hotkey (design 2026-07-11 section 4). Window-scoped
             only: global hotkeys remain dropped (Stage 4 lock); the command's CanExecute
             already gates to Recording/Paused. -->
        <KeyBinding Modifiers="Ctrl+Shift" Key="M"
                    Command="{Binding Session.MuteLocalCommand}" />
    </Window.InputBindings>
    <DockPanel TextElement.Foreground="{DynamicResource TextFillColorPrimaryBrush}">
        <ui:TitleBar DockPanel.Dock="Top" Title="LocalScribe - Record console"
                     ShowMinimize="True" ShowMaximize="True" ShowClose="True" />
        <Grid Margin="12">
            <!-- Idle: the record console (design 5.4 section 6) -->
            <StackPanel MaxWidth="420" VerticalAlignment="Center"
                        Visibility="{Binding Session.IsIdle, Converter={StaticResource BoolToVis}}">
                <TextBlock Text="Ready to record" FontSize="20" FontWeight="SemiBold"
                           HorizontalAlignment="Center" Margin="0,0,0,12" />
                <!-- Record-console polish (design 2026-07-13 section 5 item 5): the pre-flight
                     target line replaces the two grey summary lines that duplicated the pickers
                     below. Informational ONLY - it never gates or delays Start. Refreshed by the
                     existing 2 s visible-only poll in LiveViewWindow.xaml.cs. -->
                <TextBlock Text="{Binding Console.PreflightSummary}" Style="{StaticResource MutedText}"
                           TextWrapping="Wrap" TextAlignment="Center" HorizontalAlignment="Center" />
                <!-- Item 4: the engine chip - the model+backend Start WILL bind (settings +
                     BackendSelector via SessionController.PreviewEnginePlan). Hidden until the
                     first refresh fills it so no empty pill flashes. -->
                <Border Background="{DynamicResource ControlFillColorSecondaryBrush}" CornerRadius="10"
                        Padding="10,3" HorizontalAlignment="Center" Margin="0,6,0,12">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Console.EngineSummary}" Value="">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <TextBlock Text="{Binding Console.EngineSummary}" Style="{StaticResource MutedText}" />
                </Border>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,12">
                    <TextBlock Text="Microphone" VerticalAlignment="Center" Margin="0,0,8,0" />
                    <ComboBox MinWidth="220" ItemsSource="{Binding Console.MicChoices}"
                              DisplayMemberPath="Label"
                              SelectedItem="{Binding Console.SelectedMic, Mode=TwoWay}" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,4">
                    <TextBlock Text="Remote target" VerticalAlignment="Center" Margin="0,0,8,0" />
                    <ComboBox MinWidth="240"
                              ItemsSource="{Binding Console.RemoteTargetOptions}"
                              SelectedItem="{Binding Console.SelectedRemoteTarget, Mode=OneWay}"
                              DisplayMemberPath="Label"
                              SelectionChanged="RemoteTargetCombo_SelectionChanged"
                              DropDownOpened="RemoteTargetCombo_DropDownOpened" />
                </StackPanel>
                <TextBlock Text="Applies to this recording only. The saved default is unchanged (Settings &gt; Recording)."
                           Style="{StaticResource MutedText}" TextWrapping="Wrap" TextAlignment="Center"
                           HorizontalAlignment="Center" Margin="0,0,0,16" />
                <!-- Stage 6.2 Task 7: optional matter picker. Ticking a matter biases this
                     recording's Whisper prompt + seeds meta.MatterIds; picks are per-session
                     (cleared on Idle), never saved to Settings. -->
                <TextBlock Text="Matters (optional)" FontWeight="SemiBold" Margin="0,0,0,4"
                           HorizontalAlignment="Center" />
                <ui:TextBox PlaceholderText="Search matters" MinWidth="240" HorizontalAlignment="Center"
                            Text="{Binding Console.MatterPickerQuery, UpdateSourceTrigger=PropertyChanged}" />
                <Border BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}" BorderThickness="1"
                        CornerRadius="4" MaxHeight="120" Margin="0,4,0,0" HorizontalAlignment="Center" MinWidth="240">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding Console.MatterOptions}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <CheckBox Content="{Binding Display}" IsChecked="{Binding IsSelected, Mode=OneWay}"
                                              Margin="4,2"
                                              Command="{Binding DataContext.Console.ToggleMatterCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                              CommandParameter="{Binding}" />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Border>
                <TextBlock Text="{Binding Console.SelectedMatterSummary}" Style="{StaticResource MutedText}"
                           TextWrapping="Wrap" TextAlignment="Center" HorizontalAlignment="Center"
                           Margin="0,4,0,16" />
                <ui:Button Appearance="Primary" Content="Start recording" HorizontalAlignment="Center"
                           Command="{Binding Session.StartCommand}">
                    <ui:Button.Icon>
                        <ui:SymbolIcon Symbol="Record24" />
                    </ui:Button.Icon>
                </ui:Button>
            </StackPanel>
            <!-- Recording/Paused: the existing live transcript + controls, hidden when idle -->
            <DockPanel>
                <DockPanel.Style>
                    <Style TargetType="DockPanel">
                        <Setter Property="Visibility" Value="Visible" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Session.IsIdle}" Value="True">
                                <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </DockPanel.Style>
                <StackPanel DockPanel.Dock="Top" Margin="0,0,0,8">
                    <!-- Item 2: the recording state is the app's most safety-critical status -
                         promoted to a large red-accented header; Paused (and the brief Finalizing)
                         render equally large in the caution color. Item 3: Local/Remote mini
                         meters reuse the overlay pill's EXACT level source (SessionViewModel
                         LocalLevel/RemoteLevel fed by SessionController.PeakObserved, decayed by
                         the existing 150 ms TimerTick) - a silent leg visibly flatlines. -->
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                        <Ellipse Width="12" Height="12" VerticalAlignment="Center" Margin="0,0,8,0">
                            <Ellipse.Style>
                                <Style TargetType="Ellipse">
                                    <Setter Property="Fill" Value="{DynamicResource SystemFillColorCautionBrush}" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Session.State}" Value="Recording">
                                            <Setter Property="Fill" Value="{DynamicResource SystemFillColorCriticalBrush}" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Ellipse.Style>
                        </Ellipse>
                        <TextBlock Text="{Binding Session.State}" FontSize="22" FontWeight="SemiBold"
                                   VerticalAlignment="Center" Margin="0,0,12,0">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="{DynamicResource SystemFillColorCautionBrush}" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Session.State}" Value="Recording">
                                            <Setter Property="Foreground" Value="{DynamicResource SystemFillColorCriticalBrush}" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <TextBlock Text="{Binding Session.Elapsed}" FontSize="22" FontFamily="Consolas"
                                   VerticalAlignment="Center" Margin="0,0,16,0" />
                        <StackPanel VerticalAlignment="Center">
                            <StackPanel Orientation="Horizontal" Margin="0,1">
                                <TextBlock Text="Local" Style="{StaticResource MutedText}" FontSize="10"
                                           Width="42" VerticalAlignment="Center" />
                                <ProgressBar Width="64" Height="4" Maximum="1"
                                             Value="{Binding Session.LocalLevel.Value, Mode=OneWay}" />
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,1">
                                <TextBlock Text="Remote" Style="{StaticResource MutedText}" FontSize="10"
                                           Width="42" VerticalAlignment="Center" />
                                <ProgressBar Width="64" Height="4" Maximum="1"
                                             Value="{Binding Session.RemoteLevel.Value, Mode=OneWay}" />
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                    <!-- Item 4: engine chip + LIVE keep-up chip. The keep-up chip absorbs the old
                         one-shot "transcription lagging" TextBlock (removed above): green-path
                         "Keeping up OK", red "Lagging x1.4" while the rolling realtime factor is
                         above 1.0, recovering when a ladder downgrade catches up. -->
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                        <Border Background="{DynamicResource ControlFillColorSecondaryBrush}"
                                CornerRadius="10" Padding="10,3" Margin="0,0,8,0" VerticalAlignment="Center">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Session.EngineChipText}" Value="">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <TextBlock Text="{Binding Session.EngineChipText}" Style="{StaticResource MutedText}" />
                        </Border>
                        <Border CornerRadius="10" Padding="10,3" VerticalAlignment="Center">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="{DynamicResource ControlFillColorSecondaryBrush}" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Session.KeepUpLagging}" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource SystemFillColorCriticalBackgroundBrush}" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <TextBlock Text="{Binding Session.KeepUpText}">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock" BasedOn="{StaticResource MutedText}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Session.KeepUpLagging}" Value="True">
                                                <Setter Property="Foreground" Value="{DynamicResource SystemFillColorCriticalBrush}" />
                                                <Setter Property="FontWeight" Value="SemiBold" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Border>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                    <Button Style="{StaticResource PillButton}"
                            Command="{Binding Session.PauseResumeCommand}">
                        <StackPanel Orientation="Horizontal">
                            <ui:SymbolIcon FontSize="18" Margin="0,0,6,0">
                                <ui:SymbolIcon.Style>
                                    <Style TargetType="ui:SymbolIcon">
                                        <Setter Property="Symbol" Value="Pause24" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Session.State}" Value="Paused">
                                                <Setter Property="Symbol" Value="Play24" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </ui:SymbolIcon.Style>
                            </ui:SymbolIcon>
                            <TextBlock VerticalAlignment="Center">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Text" Value="Pause" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Session.State}" Value="Paused">
                                                <Setter Property="Text" Value="Resume" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding Session.StopCommand}">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource PillButton}">
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="{DynamicResource SystemFillColorCriticalBackgroundBrush}" />
                                        <Setter Property="BorderBrush" Value="{DynamicResource SystemFillColorCriticalBrush}" />
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <StackPanel Orientation="Horizontal">
                            <ui:SymbolIcon Symbol="Stop24" FontSize="18" Margin="0,0,6,0" />
                            <TextBlock Text="Stop" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding Session.MuteLocalCommand}"
                            ToolTip="Mute my side (Ctrl+Shift+M)">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource PillButton}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Session.IsLocalMuted}" Value="True">
                                        <Setter Property="Background" Value="{DynamicResource AccentFillColorDefaultBrush}" />
                                        <Setter Property="Foreground" Value="{DynamicResource TextOnAccentFillColorPrimaryBrush}" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <StackPanel Orientation="Horizontal">
                            <ui:SymbolIcon FontSize="18" Margin="0,0,6,0">
                                <ui:SymbolIcon.Style>
                                    <Style TargetType="ui:SymbolIcon">
                                        <Setter Property="Symbol" Value="Mic24" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Session.IsLocalMuted}" Value="True">
                                                <Setter Property="Symbol" Value="MicOff24" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </ui:SymbolIcon.Style>
                            </ui:SymbolIcon>
                            <TextBlock VerticalAlignment="Center">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Text" Value="Mute my side" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Session.IsLocalMuted}" Value="True">
                                                <Setter Property="Text" Value="Unmute" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </StackPanel>
                    </Button>
                    </StackPanel>
                </StackPanel>
                <!-- Capture Scope Control (design 2026-07-12 section 4): mid-recording remote-target
                     picker. Non-system-mix picks apply instantly; -> System mix pops the confirm
                     dialog first. 2-column Grid (text *, control Auto) so it does not clip at
                     MinWidth 420, mirroring the app-mute banner fix. -->
                <Grid DockPanel.Dock="Top" Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Remote target" VerticalAlignment="Center" />
                    <ComboBox Grid.Column="1" MinWidth="220"
                              ItemsSource="{Binding Console.RemoteTargetOptions}"
                              SelectedItem="{Binding Console.SelectedRemoteTarget, Mode=OneWay}"
                              DisplayMemberPath="Label"
                              SelectionChanged="RemoteTargetCombo_SelectionChanged"
                              DropDownOpened="RemoteTargetCombo_DropDownOpened" />
                </Grid>
                <!-- Task 8 / Fix #2: persistent silent-leg warning row, one line per flagged leg
                     (mic vs remote), shown while SessionController.SilentLegDetected is still
                     active for that leg and dismissed the instant SilentLegCleared fires. -->
                <StackPanel DockPanel.Dock="Top" Margin="0,0,0,8">
                    <TextBlock Text="Your side is muted - not being recorded."
                               FontWeight="SemiBold"
                               Visibility="{Binding Session.IsLocalMuted, Converter={StaticResource BoolToVis}}" />
                    <TextBlock Text="No speech detected from the microphone - check the selected device (Settings &gt; Recording)."
                               Visibility="{Binding Session.MicSilent, Converter={StaticResource BoolToVis}}"
                               Style="{StaticResource WarningText}" TextWrapping="Wrap" />
                    <TextBlock Text="No speech detected from the remote/system audio - check that meeting audio is actually playing."
                               Visibility="{Binding Session.RemoteSilent, Converter={StaticResource BoolToVis}}"
                               Style="{StaticResource WarningText}" TextWrapping="Wrap" />
                    <TextBlock Text="Your microphone device is muted - nothing is being recorded from it."
                               Visibility="{Binding Session.MicDeviceMuted, Converter={StaticResource BoolToVis}}"
                               Style="{StaticResource WarningText}" TextWrapping="Wrap" />
                    <!-- Task 8 (Phase 2 advisory app-mute banner, design 2026-07-11): the Win11 tray
                         call-mute signal mismatched the recording state for >= 5 s. ADVISORY - the
                         one-click button routes through the SAME MuteLocalCommand, so any transcript
                         marker comes from the user's click, never from this signal. -->
                    <!-- Fix 1: a 2-column Grid, NOT a horizontal StackPanel. Inside a horizontal
                         StackPanel the TextBlock is measured with infinite width, so TextWrapping is
                         inert and at MinWidth 420 / default 640 the copy truncates and the action
                         button clips off the right edge. Column 0 (*) lets the text wrap within the
                         available width; column 1 (Auto) keeps the button on-screen. -->
                    <Grid Visibility="{Binding Session.AppMuteBannerVisible, Converter={StaticResource BoolToVis}}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{Binding Session.AppMuteBannerText}"
                                   Style="{StaticResource WarningText}" TextWrapping="Wrap"
                                   VerticalAlignment="Center" />
                        <Button Grid.Column="1" Style="{StaticResource PillButton}" Margin="8,0,0,0"
                                VerticalAlignment="Center"
                                Command="{Binding Session.MuteLocalCommand}"
                                Content="{Binding Session.AppMuteActionLabel}" />
                    </Grid>
                </StackPanel>
                <Grid>
                <ListView x:Name="LineList" ItemsSource="{Binding Lines.Lines}"
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                          VirtualizingPanel.IsVirtualizing="True"
                          VirtualizingPanel.VirtualizationMode="Recycling"
                          VirtualizingPanel.ScrollUnit="Pixel"
                          ScrollViewer.CanContentScroll="True">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            <Setter Property="Margin" Value="0,0,0,10" />
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <TextBlock FontWeight="SemiBold" TextWrapping="Wrap">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsMarker}" Value="True">
                                                    <Setter Property="FontStyle" Value="Italic" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                    <Run Text="{Binding Timestamp, Mode=OneWay, StringFormat='[{0}]'}" />
                                    <Run Text="{Binding Speaker, Mode=OneWay}" />
                                </TextBlock>
                                <TextBlock Text="{Binding Text, Mode=OneWay}" TextWrapping="Wrap" Margin="0,2,0,0">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsMarker}" Value="True">
                                                    <Setter Property="FontStyle" Value="Italic" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                <!-- Item 1: empty state - visible ONLY while Recording with zero transcript lines
                     (Lines.ShowListeningHint); the FIRST line (segment or marker) removes it.
                     IsHitTestVisible False so it can never intercept list interaction. -->
                <TextBlock Text="Listening - transcript appears a few seconds after speech."
                           Style="{StaticResource MutedText}" TextWrapping="Wrap" TextAlignment="Center"
                           HorizontalAlignment="Center" VerticalAlignment="Center" Margin="24"
                           IsHitTestVisible="False"
                           Visibility="{Binding Lines.ShowListeningHint, Converter={StaticResource BoolToVis}}" />
                </Grid>
            </DockPanel>
        </Grid>
    </DockPanel>
</ui:FluentWindow>
