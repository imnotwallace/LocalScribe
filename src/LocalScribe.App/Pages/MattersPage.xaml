<Page x:Class="LocalScribe.App.Pages.MattersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:controls="clr-namespace:LocalScribe.App.Controls"
      xmlns:vm="clr-namespace:LocalScribe.App.ViewModels"
      Title="Matters">
    <Page.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
        <!-- Mirrors SessionsPage's Chip style (design 2026-07-18 section 4): same look for the
             Summary column's Done/Stale chip. -->
        <Style x:Key="Chip" TargetType="Border">
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="Padding" Value="6,1" />
            <Setter Property="Margin" Value="0,0,4,0" />
            <Setter Property="Background" Value="{DynamicResource ControlFillColorSecondaryBrush}" />
        </Style>
    </Page.Resources>
    <Grid Margin="12" TextElement.Foreground="{DynamicResource TextFillColorPrimaryBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="320" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <DockPanel Grid.Column="0" Margin="0,0,12,0">
            <WrapPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
                <ui:TextBox Width="180" Margin="0,0,8,8" VerticalAlignment="Center"
                            PlaceholderText="New matter name"
                            Text="{Binding NewMatterName, UpdateSourceTrigger=PropertyChanged}" />
                <ui:Button Content="New matter" Appearance="Primary" Margin="0,0,0,8" Click="OnCreateMatter" />
            </WrapPanel>
            <WrapPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
                <!-- Stage 5.4 5.3: search filters the list below by name, reference, or id,
                     composing with Show archived. -->
                <ui:TextBox Width="180" Margin="0,0,12,8" VerticalAlignment="Center"
                            PlaceholderText="Search matters..."
                            Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />
                <CheckBox Content="Show archived" IsChecked="{Binding ShowArchived}"
                          VerticalAlignment="Center" Margin="0,0,12,8" />
            </WrapPanel>
            <ListView x:Name="MatterList" ItemsSource="{Binding Matters}"
                      SelectionChanged="OnMatterSelected"
                      VirtualizingPanel.IsVirtualizing="True"
                      VirtualizingPanel.VirtualizationMode="Recycling">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="0,2">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock FontWeight="SemiBold" Text="{Binding Name, Mode=OneWay}" Margin="0,0,8,0" />
                                <TextBlock Text="archived" FontSize="11" Style="{StaticResource WarningText}" VerticalAlignment="Center"
                                           Visibility="{Binding Archived, Converter={StaticResource BoolToVis}}" />
                            </StackPanel>
                            <TextBlock FontSize="11" Opacity="0.7">
                                <Run Text="{Binding Id, Mode=OneWay}" />
                                <Run Text="{Binding Reference, Mode=OneWay}" />
                                <Run Text="{Binding SessionCount, Mode=OneWay, StringFormat='{}{0} session(s)'}" />
                            </TextBlock>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </DockPanel>

        <Grid Grid.Column="1" Visibility="{Binding HasSelection, Converter={StaticResource BoolToVis}}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" MinWidth="200" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition x:Name="PanelColumn" Width="0" />
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!-- Compact display-only header (design 2026-07-18 section 4): name, reference,
                     client/member chip, created date. Editing lives in the Details tab. -->
                <WrapPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
                    <TextBlock Text="{Binding EditName}" FontSize="16" FontWeight="SemiBold"
                               Margin="0,0,12,0" VerticalAlignment="Center" />
                    <TextBlock Text="{Binding EditReference}" Opacity="0.7" Margin="0,0,12,0"
                               VerticalAlignment="Center" />
                    <Border Background="{DynamicResource ControlFillColorSecondaryBrush}"
                            CornerRadius="10" Padding="8,2" Margin="0,0,12,0" VerticalAlignment="Center">
                        <TextBlock Text="{Binding HeaderSummary}" FontSize="12" />
                    </Border>
                    <TextBlock Text="{Binding HeaderCreatedDisplay}" Opacity="0.7" VerticalAlignment="Center" />
                    <ToggleButton Content="Ask" Margin="12,0,0,0"
                                  ToolTip="Ask the assistant about this matter's summaries"
                                  IsChecked="{Binding Assistant.Panel.IsOpen, Mode=TwoWay, FallbackValue=False}"
                                  Click="OnAskToggleClick" />
                </WrapPanel>

                <!-- Tabs (design 2026-07-18 section 4): Details | Sessions | Vocabulary | Advanced,
                     opening on Sessions (SelectedIndex=1). Each tab manages its own scrolling - the
                     old whole-pane ScrollViewer is gone. -->
                <TabControl Grid.Row="1" SelectedIndex="1">
                <TabControl.Template>
                    <ControlTemplate TargetType="TabControl">
                        <DockPanel>
                            <!-- One-row header (addendum): TabPanel wraps by design, so replace it
                                 with a horizontal StackPanel in a scroller - headers never stack
                                 into the broken-looking two-row grid, however narrow the panel
                                 makes the detail column. TabItem styling (WPF-UI) is untouched. -->
                            <ScrollViewer DockPanel.Dock="Top" HorizontalScrollBarVisibility="Auto"
                                          VerticalScrollBarVisibility="Disabled">
                                <StackPanel Orientation="Horizontal" IsItemsHost="True" />
                            </ScrollViewer>
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}">
                                <ContentPresenter ContentSource="SelectedContent" />
                            </Border>
                        </DockPanel>
                    </ControlTemplate>
                </TabControl.Template>
                <TabItem Header="Details">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel Margin="0,8,0,0">
                            <ui:Card Style="{StaticResource SectionCard}">
                                <StackPanel>
                                    <TextBlock Text="Details" FontWeight="SemiBold" Margin="0,0,0,8" />
                                    <TextBlock Text="Name" FontWeight="SemiBold" />
                                    <TextBox Text="{Binding EditName, UpdateSourceTrigger=PropertyChanged}"
                                             LostFocus="OnDetailCommit" Margin="0,0,0,8" />
                                    <TextBlock Text="Reference" FontWeight="SemiBold" />
                                    <TextBox Text="{Binding EditReference, UpdateSourceTrigger=PropertyChanged}"
                                             LostFocus="OnDetailCommit" Margin="0,0,0,8" />
                                    <TextBlock Text="Description" FontWeight="SemiBold" />
                                    <TextBox Text="{Binding EditDescription, UpdateSourceTrigger=PropertyChanged}"
                                             LostFocus="OnDetailCommit" AcceptsReturn="True" TextWrapping="Wrap"
                                             MinHeight="60" Margin="0,0,0,8" />
                                    <CheckBox Content="Archived" IsChecked="{Binding EditArchived}"
                                              Click="OnDetailCommit" Margin="0,0,0,8" />
                                    <TextBlock Text="{Binding CascadeStatus}" Style="{StaticResource WarningText}" Margin="0,0,0,8" />
                                </StackPanel>
                            </ui:Card>
                            <ui:Card Style="{StaticResource SectionCard}">
                                <StackPanel>
                                    <TextBlock Text="Roster" FontWeight="SemiBold" Margin="0,0,0,8" />
                                    <ItemsControl ItemsSource="{Binding Roster}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal" Margin="0,2">
                                                    <TextBox Width="180" Text="{Binding Name, Mode=OneWay}" Tag="{Binding Id}"
                                                             LostFocus="OnMemberRename" Margin="0,0,8,0" />
                                                    <TextBlock Text="{Binding Role, Mode=OneWay}" VerticalAlignment="Center" Margin="0,0,8,0" />
                                                    <ui:Button Content="Remove" Appearance="Secondary" Tag="{Binding Id}" Click="OnMemberRemove" />
                                                </StackPanel>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                    <WrapPanel Orientation="Horizontal" Margin="0,4,0,0">
                                        <ui:TextBox Width="140" PlaceholderText="Name" Margin="0,0,8,8"
                                                    Text="{Binding NewMemberName, UpdateSourceTrigger=PropertyChanged}" />
                                        <ui:TextBox Width="100" PlaceholderText="Role" Margin="0,0,8,8"
                                                    Text="{Binding NewMemberRole, UpdateSourceTrigger=PropertyChanged}" />
                                        <ui:Button Content="Add member" Appearance="Primary" Margin="0,0,0,8" Click="OnAddMember" />
                                    </WrapPanel>
                                </StackPanel>
                            </ui:Card>
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>
                <TabItem Header="Sessions">
                    <DockPanel Margin="0,8,0,0">
                        <WrapPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
                            <ui:Button Content="Add sessions..." Appearance="Primary"
                                       Click="OnAddSessions" Margin="0,0,8,8" />
                            <ui:Button Content="Open" Appearance="Secondary" Margin="0,0,8,8"
                                       ToolTip="Open the transcript read view"
                                       IsEnabled="{Binding HasTaggedSelection}" Click="OnOpenTranscript" />
                            <ui:Button Content="Details" Appearance="Secondary" Margin="0,0,8,8"
                                       ToolTip="Open Session Details (title, speakers, matters)"
                                       IsEnabled="{Binding HasTaggedSelection}" Click="OnOpenDetails" />
                            <ui:Button Content="Untag" Appearance="Secondary" Margin="0,0,8,8"
                                       IsEnabled="{Binding HasTaggedSelection}" Click="OnUntagSelected" />
                            <ui:TextBox Width="180" Margin="0,0,0,8" VerticalAlignment="Center"
                                        PlaceholderText="Filter by title..."
                                        Text="{Binding TaggedFilterText, UpdateSourceTrigger=PropertyChanged}" />
                        </WrapPanel>
                        <controls:PagerControl DockPanel.Dock="Bottom" Margin="0,8,0,0"
                                               DataContext="{Binding TaggedPager}" />
                        <ui:DataGrid x:Name="TaggedGrid" ItemsSource="{Binding TaggedSessions}"
                                     SelectedItem="{Binding SelectedTagged}"
                                     AutoGenerateColumns="False" IsReadOnly="True"
                                     CanUserAddRows="False" GridLinesVisibility="None"
                                     HeadersVisibility="Column" SelectionMode="Single"
                                     VirtualizingPanel.IsVirtualizing="True"
                                     VirtualizingPanel.VirtualizationMode="Recycling"
                                     MouseDoubleClick="OnTaggedRowDoubleClick">
                            <ui:DataGrid.Columns>
                                <DataGridTextColumn Header="Title" Width="*"
                                                    Binding="{Binding Title, Mode=OneWay}" />
                                <DataGridTextColumn Header="Date" Width="Auto"
                                                    Binding="{Binding DateDisplay, Mode=OneWay}" />
                                <DataGridTextColumn Header="Duration" Width="Auto"
                                                    Binding="{Binding DurationDisplay, Mode=OneWay}" />
                                <DataGridTemplateColumn Header="Summary" Width="110" MinWidth="90">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <WrapPanel VerticalAlignment="Center">
                                                <!-- Done/Stale: a chip; click opens the read view's panel.
                                                     Stale gets the caution tint + tooltip (surfaced, never
                                                     silent). Null status renders NOTHING (unknown). -->
                                                <Button x:Name="SummaryChip" Visibility="Collapsed"
                                                        Click="OnOpenSummary" Padding="0" Margin="0"
                                                        Background="Transparent" BorderThickness="0"
                                                        Cursor="Hand">
                                                    <Border x:Name="ChipBorder" Style="{StaticResource Chip}">
                                                        <TextBlock Text="Summary" />
                                                    </Border>
                                                </Button>
                                                <Button x:Name="GenerateLink" Visibility="Collapsed"
                                                        Click="OnGenerateSummary" Padding="0" Margin="0"
                                                        Background="Transparent" BorderThickness="0"
                                                        Cursor="Hand"
                                                        ToolTip="Generate an AI summary draft for this session">
                                                    <TextBlock Text="Generate" FontSize="11" Opacity="0.7"
                                                               TextDecorations="Underline" />
                                                </Button>
                                            </WrapPanel>
                                            <DataTemplate.Triggers>
                                                <DataTrigger Binding="{Binding SummaryStatus}" Value="{x:Static vm:SummaryStatus.Done}">
                                                    <Setter TargetName="SummaryChip" Property="Visibility" Value="Visible" />
                                                    <Setter TargetName="SummaryChip" Property="ToolTip"
                                                            Value="An AI-generated draft summary exists - click to open it in the read view" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding SummaryStatus}" Value="{x:Static vm:SummaryStatus.Stale}">
                                                    <Setter TargetName="SummaryChip" Property="Visibility" Value="Visible" />
                                                    <Setter TargetName="ChipBorder" Property="Background"
                                                            Value="{DynamicResource SystemFillColorCautionBackgroundBrush}" />
                                                    <Setter TargetName="SummaryChip" Property="ToolTip"
                                                            Value="The transcript changed after this AI-generated draft summary - click to open and regenerate" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding SummaryStatus}" Value="{x:Static vm:SummaryStatus.None}">
                                                    <Setter TargetName="GenerateLink" Property="Visibility" Value="Visible" />
                                                </DataTrigger>
                                            </DataTemplate.Triggers>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </ui:DataGrid.Columns>
                        </ui:DataGrid>
                    </DockPanel>
                </TabItem>
                <TabItem Header="Vocabulary">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel Margin="0,8,0,0">
                            <ui:Card Style="{StaticResource SectionCard}">
                                <StackPanel>
                                    <TextBlock Text="Custom vocabulary" FontWeight="SemiBold" Margin="0,0,0,8" />
                                    <TextBlock Style="{StaticResource Note}" TextWrapping="Wrap"
                                               Text="Terms and corrections scoped to this matter (client names, case jargon). They add to the global vocabulary; a matter correction overrides a same-word global one. New recordings tagged with this matter get the term bias; already-recorded sessions update only when you re-render them below." />

                                    <TextBlock Text="Terms" FontWeight="SemiBold" Margin="0,8,0,4" />
                                    <ItemsControl ItemsSource="{Binding Vocabulary.Terms}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                                                    <TextBlock Text="{Binding Text}" VerticalAlignment="Center" MinWidth="220" />
                                                    <Button Content="Remove"
                                                            Command="{Binding DataContext.Vocabulary.RemoveTermCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                            CommandParameter="{Binding}" />
                                                </StackPanel>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                    <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                        <TextBox MinWidth="220"
                                                 Text="{Binding Vocabulary.NewTerm, UpdateSourceTrigger=PropertyChanged}" />
                                        <Button Content="Add" Margin="8,0,0,0"
                                                Command="{Binding Vocabulary.AddTermCommand}" />
                                    </StackPanel>

                                    <TextBlock Text="Corrections (heard -&gt; correct)" FontWeight="SemiBold" Margin="0,12,0,4" />
                                    <ItemsControl ItemsSource="{Binding Vocabulary.Corrections}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                                                    <TextBlock Text="{Binding Heard}" VerticalAlignment="Center" MinWidth="120" />
                                                    <TextBlock Text="-&gt;" VerticalAlignment="Center" Margin="4,0" />
                                                    <TextBlock Text="{Binding Correct}" VerticalAlignment="Center" MinWidth="120" />
                                                    <Button Content="Remove" Margin="8,0,0,0"
                                                            Command="{Binding DataContext.Vocabulary.RemoveCorrectionCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                            CommandParameter="{Binding}" />
                                                </StackPanel>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                    <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                        <TextBox MinWidth="120"
                                                 Text="{Binding Vocabulary.NewHeard, UpdateSourceTrigger=PropertyChanged}" />
                                        <TextBox MinWidth="120" Margin="8,0,0,0"
                                                 Text="{Binding Vocabulary.NewCorrect, UpdateSourceTrigger=PropertyChanged}" />
                                        <Button Content="Add" Margin="8,0,0,0"
                                                Command="{Binding Vocabulary.AddCorrectionCommand}" />
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                                        <ui:Button Content="Re-render tagged sessions" Click="OnRerenderTagged" />
                                        <TextBlock Text="{Binding CascadeStatus}" Style="{StaticResource WarningText}"
                                                   VerticalAlignment="Center" Margin="8,0,0,0" />
                                    </StackPanel>
                                </StackPanel>
                            </ui:Card>
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>
                <TabItem Header="Advanced">
                    <StackPanel Margin="0,8,0,0">
                        <ui:Card Style="{StaticResource SectionCard}">
                            <StackPanel>
                                <TextBlock Text="Advanced" FontWeight="SemiBold" Margin="0,0,0,8" />
                                <ui:Button Content="Repair index" Appearance="Secondary" HorizontalAlignment="Left"
                                           ToolTip="Rebuilds the matter-to-session index if the list looks out of date."
                                           Click="OnRepairIndex" />
                            </StackPanel>
                        </ui:Card>

                        <ui:Button Content="Export matter archive..." Appearance="Secondary" Click="OnExportMatter"
                                   HorizontalAlignment="Left" Margin="0,4,0,0"
                                   ToolTip="Bundles this matter's tagged sessions (and a matter.json snapshot) into one .zip." />
                        <StackPanel Orientation="Horizontal" Margin="0,4,0,0"
                                    Visibility="{Binding IsExporting, Converter={StaticResource BoolToVis}}">
                            <ProgressBar Width="200" Minimum="0" Maximum="{Binding ExportMax}" Value="{Binding ExportProgress}"
                                         VerticalAlignment="Center" Margin="0,0,8,0" />
                            <ui:Button Content="Cancel" Appearance="Secondary" Click="OnCancelExport" />
                        </StackPanel>

                        <ui:Button Content="Delete matter" Appearance="Secondary" Click="OnDeleteMatter"
                                   HorizontalAlignment="Left" Margin="0,4,0,0" />
                    </StackPanel>
                </TabItem>
                </TabControl>
            </Grid>
            <!-- Chat-only assistant panel (Phase 3): matter summaries are surfaced via the
                 Sessions tab's Summary column now, not inside the panel - the old Assistant tab's
                 coverage/status surfaces are gone, replaced by Panel.CoverageText inside the panel
                 itself (MatterAssistantViewModel.UpdateCoverage writes both). FallbackValue=Collapsed
                 because Assistant is null until a matter with a composed assistant stack is selected. -->
            <GridSplitter Grid.Column="1" Width="6" HorizontalAlignment="Stretch"
                          VerticalAlignment="Stretch" ResizeBehavior="PreviousAndNext"
                          Background="{DynamicResource ControlFillColorSecondaryBrush}"
                          Visibility="{Binding Assistant.Panel.IsOpen, Converter={StaticResource BoolToVis}, FallbackValue=Collapsed}" />
            <controls:AssistantSidePanel Grid.Column="2" DataContext="{Binding Assistant.Panel}"
                          Visibility="{Binding Assistant.Panel.IsOpen, Converter={StaticResource BoolToVis}, FallbackValue=Collapsed}" />
        </Grid>
    </Grid>
</Page>
