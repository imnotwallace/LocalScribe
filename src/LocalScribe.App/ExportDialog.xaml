<Window x:Class="LocalScribe.App.ExportDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:LocalScribe.App.ViewModels"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:local="clr-namespace:LocalScribe.App"
        Title="Export session" Width="420" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
        <local:InverseBooleanConverter x:Key="InverseBool" />
    </Window.Resources>
    <StackPanel Margin="16">
        <!-- Dialog-local feedback (Tier 1 plan D, T1-5, 2026-08-05): range refusals, write
             failures and cancellation surface HERE, not on MainWindow's InfoBar this separate
             dialog cannot show (the same trap the read-view save fix and the Split-speakers fix
             already answered). IsClosable=False: state-driven, cleared on the next attempt.
             Severity rides a Style+DataTrigger because InfoBar.Severity is an enum DP that
             cannot bind a bool. -->
        <ui:InfoBar Margin="0,0,0,8" IsClosable="False"
                    IsOpen="{Binding HasStatus, Mode=OneWay}"
                    Message="{Binding StatusMessage}">
            <ui:InfoBar.Style>
                <Style TargetType="{x:Type ui:InfoBar}">
                    <Setter Property="Severity" Value="Informational" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding StatusIsError}" Value="True">
                            <Setter Property="Severity" Value="Error" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ui:InfoBar.Style>
        </ui:InfoBar>
        <TextBlock Text="Format" FontWeight="SemiBold" Margin="0,0,0,4" />
        <RadioButton Content="Zip archive (audio + transcript + metadata)" GroupName="Fmt"
                     IsChecked="{Binding Format, Converter={x:Static vm:ExportFormatToBool.Zip}}" Margin="0,2" />
        <RadioButton Content="Word document (.docx transcript)" GroupName="Fmt"
                     IsChecked="{Binding Format, Converter={x:Static vm:ExportFormatToBool.Docx}}" Margin="0,2" />
        <RadioButton Content="Markdown (.md transcript)" GroupName="Fmt"
                     IsChecked="{Binding Format, Converter={x:Static vm:ExportFormatToBool.Markdown}}" Margin="0,2" />
        <RadioButton Content="Plain text (.txt transcript)" GroupName="Fmt"
                     IsChecked="{Binding Format, Converter={x:Static vm:ExportFormatToBool.Text}}" Margin="0,2" />
        <!-- The toggles apply to ALL THREE textual formats (design 2026-07-18 section 3 +
             2026-08-04 section 3): docx, markdown AND plain text; hidden for zip, which archives
             the session folder as-is. The cadence checkbox is subordinate to Include timestamps
             (plain bool IsEnabled binding - no inverting converter needed, per house rule). -->
        <StackPanel Margin="16,8,0,0" Visibility="{Binding ShowOptionToggles, Converter={StaticResource BoolToVis}}">
            <!-- IsChecked binds the DISPLAY property (TimestampsChecked), not IncludeTimestamps
                 itself: an excerpt visually forces this on without overwriting the user's real,
                 persisted preference (review finding 2026-08-04). The cadence sub-controls follow
                 the same display value, since a force-checked "Include timestamps" makes them
                 genuinely available for this export too. -->
            <CheckBox Content="Include timestamps" IsChecked="{Binding TimestampsChecked}"
                      IsEnabled="{Binding TimestampsToggleEnabled}" Margin="0,2" />
            <StackPanel Orientation="Horizontal" Margin="16,2,0,2">
                <CheckBox Content="Extra timestamp every" IsChecked="{Binding ExtraTimestamps}"
                          IsEnabled="{Binding TimestampsChecked}" VerticalAlignment="Center" />
                <ComboBox ItemsSource="{Binding CadenceChoices}" DisplayMemberPath="Label"
                          SelectedValuePath="Ms" SelectedValue="{Binding SelectedCadenceMs}"
                          IsEnabled="{Binding TimestampsChecked}" Width="80" Margin="8,0,0,0" />
            </StackPanel>
            <CheckBox Content="Include system markers" IsChecked="{Binding IncludeMarkers}" Margin="0,2" />
            <CheckBox Content="Include assistant summary" IsChecked="{Binding IncludeSummary}" Margin="0,2" />
            <CheckBox Content="Mark corrected turns" IsChecked="{Binding MarkCorrectedTurns}" Margin="0,2"
                      ToolTip="Flags each turn whose text a person corrected. On by default - an exported transcript that hides its edits reads as concealment." />
        </StackPanel>
        <!-- Time-range excerpt (design 2026-08-04 section 8). The range is NEVER remembered
             (VM constructor never seeds it from settings, PersistChoicesAsync never saves it): a
             remembered range would silently emit a partial export of the next, unrelated session.
             Plain TextBoxes, deliberately not the read view's auto-colon masked go-to box - that
             mask carried an unpadded-paste defect where "1:02:03" normalised to ten hours;
             TimestampParser handles that string correctly, the defect was in the mask. -->
        <StackPanel Margin="0,8,0,0" Visibility="{Binding ShowOptionToggles, Converter={StaticResource BoolToVis}}">
            <CheckBox Content="Export a time range only" IsChecked="{Binding ExcerptEnabled}" Margin="0,2" />
            <StackPanel Orientation="Horizontal" Margin="16,2,0,2"
                        Visibility="{Binding ExcerptEnabled, Converter={StaticResource BoolToVis}}">
                <TextBlock Text="From" VerticalAlignment="Center" Margin="0,0,6,0" />
                <TextBox Text="{Binding ExcerptFrom, UpdateSourceTrigger=PropertyChanged}" Width="80" />
                <TextBlock Text="To" VerticalAlignment="Center" Margin="12,0,6,0" />
                <TextBox Text="{Binding ExcerptTo, UpdateSourceTrigger=PropertyChanged}" Width="80" />
            </StackPanel>
        </StackPanel>
        <!-- Indeterminate, deliberately: no export path exposes a fraction. The bar exists so a
             multi-gigabyte .zip does not look like a frozen dialog, and Stop exists so it can
             actually be abandoned. -->
        <ProgressBar Height="4" Margin="0,12,0,0" IsIndeterminate="True">
            <ProgressBar.Style>
                <Style TargetType="ProgressBar">
                    <Setter Property="Visibility" Value="Collapsed" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsBusy}" Value="True">
                            <Setter Property="Visibility" Value="Visible" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ProgressBar.Style>
        </ProgressBar>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Export..." IsDefault="True" Command="{Binding ExportCommand}" Margin="0,0,8,0" MinWidth="90" />
            <!-- Two separate buttons, not one that changes role: IsCancel=True closes the window
                 on Esc whatever is bound to it, so a single button would abandon the dialog
                 mid-write instead of cancelling the export. -->
            <Button Content="Stop" Command="{Binding StopCommand}" Margin="0,0,8,0" MinWidth="90">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsBusy}" Value="True">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            <Button Content="Close" IsCancel="True" MinWidth="90"
                    IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBool}}" />
        </StackPanel>
    </StackPanel>
</Window>
